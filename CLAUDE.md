# CLAUDE.md — репозиторий `sleepover` (party-game «Комната»)

Корневой файл проекта. Читается в начале каждой сессии. **Правила написания кода — в `@igruha/CLAUDE.md`** (SOLID, Unity-специфика, сетевые правила NGO). Этот файл — про навигацию и процесс.

---

## 0. Кто есть кто

- **Пользователь** — геймдизайнер и продюсер проекта, не программист. Технические решения принимаю и объясняю я. Не отправлять его «сделать руками в редакторе», если это можно сделать через Unity MCP.
- **Напарник** — второй разработчик, делает свою мини-игру параллельно. Оба пушат в `main`.
- Общение — на русском.

## 1. Что за игра

Онлайн party-game «Комната» для ПК/Steam, 2–8 игроков. Хаб-комната + пул из 15 мини-игр, выбираемых случайно. Очки за место, победитель катки — с короной. Референсы: Pummel Party, Human: Fall Flat, Gang Beasts.

Unity 6.3, URP. Сеть — Netcode for GameObjects (NGO) + Relay, host-based, **авторитет сервера**.

## 2. Карта репозитория

```
sleepover/                         ← рабочая директория, git-репозиторий
├── CLAUDE.md                      ← этот файл (навигация + процесс)
├── STATE.md                       ← ЕДИНЫЙ статус проекта, коротко. Читать в начале, обновлять в конце сессии
├── docs/
│   ├── history/                   ← архив прошлых проходов + указатель README.md. Целиком НЕ читать
│   ├── minigames/<игра>.md        ← дизайн-спека мини-игры (вход в конвейер)
│   └── templates/                 ← шаблоны спеки и тикета
└── igruha/                        ← Unity-проект
    ├── CLAUDE.md                  ← ПРАВИЛА КОДА (обязательны)
    ├── LINEAR_ROADMAP.md          ← EPIC 0–24, общий план на месяц
    ├── docs/img/                  ← схемы планировок хаба и уровней (jpg)
    └── Assets/_Project/
        ├── Scripts/Core/          ← ОБЩИЕ системы для всех 15 мини-игр
        ├── Scripts/Minigames/     ← уникальный код конкретной игры
        ├── Scripts/Networking/    ← NGO-слой
        ├── Scenes/                ← Boot, Hub, MinigameTemplate, Minigames/<Игра>.unity
        │   └── MinigameTemplate_HOWTO.md   ← 6 шагов создания мини-игры из шаблона
        ├── Settings/Gameplay/Minigames/    ← MinigameDefinition (ScriptableObject) на игру
        └── Prefabs/
```

@igruha/CLAUDE.md

## 3. Конвейер мини-игры — главный процесс

Каждая мини-игра проходит **6 фаз строго по порядку**. Фаза не начинается, пока предыдущая не закрыта.

| # | Фаза | Скилл | Модель | Что на выходе |
|---|---|---|---|---|
| 0 | **Спека** | `/mg-spec` | Opus 5 / high | `docs/minigames/<игра>.md` — полная, без открытых вопросов, сверенная с Core |
| 1 | **Тикеты** | `/mg-tickets` | Sonnet 5 / medium | Эпик в Linear + тикеты по фазам, каждый самодостаточен |
| 2 | **Каркас (solo)** | `/mg-blockout` | Opus 5 / xhigh | Игра полностью играется в одиночку на серых блоках, без багов |
| 3 | **Сеть** | `/mg-net` | Opus 5 / xhigh | Мультиплеер под авторитетом сервера, host+client сходятся |
| 4 | **Арт** | `/mg-art` | Sonnet 5 / medium | Серые блоки заменены на модели и материалы |
| 5 | **Звук** | `/mg-sound` | Opus 5 / high | Все события игры озвучены, слышат все участники, немых слотов нет |
| — | **Проверка** | `/mg-check` | Sonnet 5 / medium | Компиляция, ревью по правилам, синхронизация Linear и STATE |

**Эпик закрыт = мини-игра полностью готова** (все шесть фаз). Только после этого берём следующую.

### Звук — почему отдельная фаза, а не хвост арта

Раньше звук был подфазой `4.5` внутри `/mg-art` и генерировался fal.ai по месту.
С 18.09.2026 звук приходит **поставками** — папка с `sounds.json` и готовыми
файлами из библиотек Sonniss и Kenney, — и живёт по своему расписанию, не
совпадающему с артом: часть игр уже оформлена, но озвучена черновиками, а часть
звуков приехала раньше, чем игра, для которой они сделаны.

Фаза делится на три скилла по ответственности:

| Скилл | Когда | Что делает |
|---|---|---|
| [`/sfx-intake`](.claude/skills/sfx-intake/SKILL.md) | пришла новая папка со звуками | раскладывает по `Assets/_Project/Audio`, дополняет манифесты, собирает библиотеки, называет недостающее |
| [`/sfx-core`](.claude/skills/sfx-core/SKILL.md) | общий звук и разметка сцен | шаги по поверхностям, прыжок, падение, удар, интерфейс, ловушки — один раз на все пятнадцать игр |
| [`/mg-sound`](.claude/skills/mg-sound/SKILL.md) | фаза 5 конкретной игры | сводит события игры со слотами, привязывает компонентом `<Игра>Audio` |

Правило привязки одно и оно важнее всего остального в фазе: **звук вешается на
событие, которое поднимается на каждой машине.** Звук, слышный только
инициатору, означает неверную привязку, а не проблему звука. Ни одного нового
RPC ради звука не заводится.

### Повторная шлифовка существующих игр

Для полной переработки арта уже играющей мини-игры использовать
[`/mg-polish`](.claude/skills/mg-polish/SKILL.md): собственные модели и модули
через Blender и другие инструменты, материалы, окружение, свет и реакции.
Пользователь дал постоянную свободу художественно переосмысливать выбранную сцену:
самостоятельно менять состав, форму, масштаб и расстановку объектов, архитектуру,
планировку и каркас уровня, включая связанную физику. Цель — красивая, цельная сцена
и лучший игровой опыт с сохранением замысла мини-игры. Старые координаты и размеры
не заморожены; замена Synty своими моделями один к одному не является целью.
При наличии референса воплощать его композицию, формы, цвета и настроение,
самостоятельно принимая решения по реализации. Отдельное разрешение на каждый
перенос или изменение каркаса не требуется; результат проверяется в игре.
Это повторный проход; фазы 0–3 заново не запускаются. Старый `/mg-art` описывает
процесс с Synty и для такой задачи не применяется. Новый арт делаем сами.

### Модель и уровень рассуждения

Связка ставится на сессию: `/model` — модель, `/config` — уровень. Открыл чат под фазу → выставил связку → дальше не трогаешь.

- **Думать и писать логику — Opus 5, перекладывать и заполнять — Sonnet 5.**
- **`xhigh` — для агентного кодинга** (каркас, сеть): лучше держит связность на многофайловой работе. **`high`** — для рассуждения без кодинга (спека). **`medium`** — для механики; на Opus 5 и Sonnet 5 низкие уровни неожиданно сильные, так что `high` на нарезке тикетов это слитый лимит и лишнее ожидание.
- **`max`** — аварийный уровень: прирост даёт не всегда, зато стабильно жжёт токены и склонен переусложнять простое.
- **Fable 5** — последняя ступень эскалации на упорном баге (обычно сетевом): вдвое дороже Opus 5 и думает дольше. По умолчанию не брать.
- **`/fast`** — тот же Opus 5 с быстрой выдачей, полезен на пачке мелких правок.

Лестница, когда застряли: `xhigh` → `max` → Fable 5.

Правила конвейера:

- **Фазу 2 пишем network-ready.** Всё, что меняет важное состояние (урон, очки, места, старт/конец, активация ловушек), — через одну точку входа, которая в фазе 3 уйдёт за `IsServer`/`ServerRpc` без переписывания логики. Состояние раунда — в структурах, которые мигрируют в `NetworkVariable`, а не в разрозненных полях MonoBehaviour.
- **Общее — в `Core/`, уникальное — в `Minigames/<Игра>/`.** Если механика может понадобиться второй игре, её место в Core. Это то, ради чего строился шаблон.
- **Ничего не хардкодить под число игроков** — всё работает на 2–8.
- Перед фазой 2 всегда перечитывать `Scenes/MinigameTemplate_HOWTO.md` — там актуальный список того, что шаблон даёт бесплатно.

## 4. Правила сессии

1. **В начале** — прочитать `STATE.md` целиком: он держится коротким (около 150 строк) именно для этого. Не переисследовать репозиторий с нуля. **История проходов — в `docs/history/`, и читать её целиком не надо**: указатель `docs/history/README.md` даёт номер строки на каждый раздел, дальше читается ровно нужный диапазон (`sed -n 'A,Bp' docs/history/STATE-archive.md`). Сплошной `grep` по архиву бесполезен: записи склеены длинными строками, одно совпадение вытаскивает соседние темы на десятки килобайт.
2. **Источник истины по задачам — Linear** (команда `IGRUHA`, проект «Комната (Party-game)»), через MCP. Локальных копий тикетов не заводим. **Статусы двигает агент, сразу по факту работы** — взял тикет в работу — `In Progress`, закрыл — `Done`. Раньше здесь стояло «статусы двигаю я», и на этом тикеты копились неделями: четыре подфазы арта «Дырки в стене» были сделаны и приняты, а в Linear висели в `Backlog` и `In Progress`. Если MCP Linear не поднялся — сказать об этом вслух, а не молча пропустить.
3. **Unity — под моим управлением через Unity MCP**: сцены, префабы, компоненты, ScriptableObject'ы, чтение консоли. Пользователю оставляем только то, что физически требует человека: плейтест, оценка «смешно/не смешно», покупка стор-ассетов.

   **Если инструментов `mcp__unity__*` в сессии нет — это почти всегда порядок запуска, а не поломка.** Клиент опрашивает MCP-серверы один раз, на старте сессии; чат, открытый раньше Unity, получает `ConnectionRefused` и остаётся без них до конца. Лечится `/mcp` после того, как редактор поднялся, — а если и это не помогло, работать через `tools/unity_bridge.py`: тот же мост напрямую, умеет всё то же самое. Пользователя переключениями не мучить.
4. **После любых изменений C#** — `refresh_unity`, затем `read_console` (errors). Не переходить к следующей задаче с ошибками компиляции.
5. **В конце сессии** — обновить `STATE.md` и статусы в Linear. Кратко резюмировать: что изменено, что проверить в редакторе, что проверить по сети.
   **`STATE.md` обязан оставаться коротким.** Свой проход описывать в разделе 1, заменяя им прошлый: содержательная часть прошлого уезжает в конец `docs/history/STATE-archive.md`, а в срезе остаётся только то, что меняет состояние — строка игры в таблице, новый долг, новые грабли. После переноса — `python3 tools/state_index.py`, он пересобирает указатель. Файл однажды дорос до 17 тысяч строк, и каждое чтение стоило сотни тысяч токенов; повторять это не нужно.
6. **Не изобретать документацию.** Один статус — `STATE.md`. Не плодить `*_SUMMARY.md`, `*_AUDIT.md`, `*_GUIDE.md` — они разъезжаются с кодом и врут (уже было с EPIC 2). `docs/history/` этому не противоречит: это журнал прошлого, а не второй статус, и текущего состояния он не описывает.

## 5. Работа вдвоём — ПРАВИЛО ВЕТОК

Формулировка ниже — окончательная. Она отменяет всё, что говорилось о ветках
раньше, в том числе запись напарника от 19.08 о «прежний запрет пушить в `main`
больше не действует». Та же формулировка дословно продублирована в `STATE.md`.

1. **Прямые коммиты в `main` запрещены всегда, без исключений** — в том числе
   для мелких багфиксов после мерджа. Любое изменение, даже однострочное, идёт
   через ветку.

2. **Работа ведётся в отдельной ветке на фичу/фазу.** Закрыл фазу, протестировал
   → мерджишь ветку в `main`.

3. **Если после мерджа в `main` находится баг** — фикс делается в новой короткой
   ветке (или в той же, если она ещё не удалена), тестируется, и мерджится в
   `main` тем же порядком. Не напрямую.

4. **`main` нужно подтягивать в свою рабочую ветку регулярно**, а не копить
   надолго — иначе слияние проверенного `main` обратно в `main` с задержкой даёт
   крупные конфликты разом (так было при слиянии 19.08 — 95 коммитов и
   9 конфликтов одновременно). Частые мелкие слияния `main` → своя ветка снижают
   риск.

5. **Если фаза трогает общие файлы** (Core, `PlayerController`, камера и т.п.,
   используемые в нескольких мини-играх) — **предупредить второго участника
   команды до мерджа в `main`**, не после.

### Что к этому прилагается

- **Одна сцена = один владелец в моменте.** `.unity` и `.prefab` не мержатся
  по-человечески. Кто делает Duck Hunt, не трогает сцену второй игры.
- **Изменения в `Core/` — отдельным мелким коммитом.** Не копить Core-правки
  внутри большой фичи.
- Коммит — на русском, по смыслу задачи, с ID тикета:
  `IGR-151: сетевой синхрон выстрелов Duck Hunt`.

### Канал предупреждений — топик `warnings` в общей ТГ-группе

Заведён 26.08. **Всё, что второй участник должен узнать раньше, чем сделает
pull, идёт туда, а не в репозиторий.** Обоюдно: он пишет нам тем же порядком.

Причина — механическая, а не организационная. Запись в `STATE.md` доезжает до
второго только цепочкой «коммит → мердж ветки в `main` → push → его pull», то
есть **приезжает тем же мерджем, о котором предупреждает**. Пока работа идёт в
ветке, наших записей для него не существует вовсе. Предупредить «до мерджа»
файлом внутри репозитория невозможно by design.

Что писать в топик:

- **до** мерджа ветки, которая трогает `Core/`, общие префабы, аниматоры,
  ассет ввода — что именно тронуто и что проверить;
- переделанную общую сцену (`Hub.unity`) — вместе с напоминанием закрыть Unity
  перед pull;
- решения, меняющие правила работы: что не кладём в репозиторий, что ставится
  из Asset Store руками.

`STATE.md` при этом остаётся журналом: он отвечает на вопрос «что я увижу,
когда волью», а не «что мне сделать прямо сейчас».

### Перед `git pull` / `merge` — закрыть Unity

Иначе редактор и git пишут в одни файлы одновременно: Unity пересоздаёт `.meta`
с новыми ID, ссылки в префабах и сценах рвутся. Проверено на практике 15.08.

### В репозитории Git LFS

Модели, текстуры, звук и видео идут через LFS (см. `.gitattributes`). Отсюда
долгие `pull`: объектов уже больше 5 ГБ, потому что каждый файл анимации несёт
внутри себя всю модель целиком (~85 МБ на клип вместо ~1 МБ). Чистка — отдельная
задача, трогает файлы обоих разработчиков, делается по договорённости.
Пока правило простое: **новые анимации класть без модели внутри**.

## 6. Полезные факты о проекте (не переоткрывать каждый раз)

- **Физика — простой Rigidbody-контроллер + анимации + impulse-толчки. НЕ active ragdoll.** Разделы GDD 6.3/6.5 про active ragdoll устарели.
- `GDD.md` и `HANDOFF.md` **не существуют** в репозитории, хотя `igruha/docs/FABLE_MASTER_PROMPT.md` и роадмап на них ссылаются. Не искать — их нет.
- Схемы уровней лежат в `igruha/docs/img/` (в FABLE_MASTER_PROMPT указан несуществующий путь `docs/images/`).
- Хаб — это **подвальная game room** (боулинг, пинг-понг, бильярд, кикер, мини-бар, дартс, диван с плазмой, пьедестал с короной), а не комната подростка из GDD. Планировка в `docs/img/` — актуальнее GDD.
- Арт существующих мини-игр перерабатываем через `/mg-polish`: собственные модели, модульные наборы и материалы в Blender и других подходящих инструментах. Новые Synty/стор-паки для этой работы не используем; ранее импортированное заменяем в рамках выбранной игры с проверкой зависимостей.
