namespace Igruha.Core.Interaction
{
    /// <summary>
    /// Пометка: взаимодействие с этим объектом ничего не меняет в общем состоянии
    /// и исполняется на месте, не уходя серверу.
    ///
    /// Нужна из-за сетевого моста: он умеет адресовать только объекты с
    /// <c>NetworkObject</c> и всё остальное честно отклоняет, чтобы состояние не
    /// разъехалось. Для якоря мини-игры это правило било мимо — нажатие открывает
    /// окно подтверждения у нажавшего, а саму игру грузит хост через отдельную
    /// проверку в HubController. Без пометки кнопка не работала вообще ни у кого.
    ///
    /// Вешать только туда, где взаимодействие — чистый локальный UI. Кнопки,
    /// двери и предметы мира к таким не относятся: им нужен сервер.
    /// </summary>
    public interface ILocalInteraction
    {
    }
}
