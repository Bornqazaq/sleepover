using UnityEngine;

namespace Igruha.Core.CameraSystems
{
    /// <summary>
    /// Маркер: рендереры под этим объектом не прячутся, когда игрок смотрит
    /// своими глазами. Вид от первого лица гасит собственную модель, иначе
    /// в кадр лезет изнанка текстур, — но не всё, что висит на персонаже,
    /// является его телом. Фонарь Водящего именно тот, кому его луч нужен
    /// виднее всех.
    ///
    /// Маркер, а не проверка типа рендерера: у персонажей и меши, и скины,
    /// и навесное снаряжение — угадывать по типу значит ошибаться на первом
    /// же новом предмете.
    /// </summary>
    public sealed class KeepVisibleInFirstPerson : MonoBehaviour
    {
    }
}
