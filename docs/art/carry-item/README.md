# CarryItem — стройка небоскрёба, IGR-535

Собственный Blender-набор заменяет Synty в сцене и всех четырёх игровых префабах.
38 FBX, исходник `igruha/Assets/_Project/Art/Source/CarrySkyscraper/CarrySkyscraper.blend`,
генератор `tools/blender/carry_skyscraper.py`. Текстуры бетона, древесины, сетки
и частиц тоже созданы в этом проходе.

## Воспроизведение

1. Запустить генератор через Blender MCP. Он меняет только свои сцены `CarrySkyscraper_*`.
2. В Unity открыть CarryItem в Edit Mode, вызвать `CarryItemArenaBuilder.Rebuild()`, сохранить сцену.
3. Вызвать `CarryItemArtAudit.Measure()`, `CarryItemTrafficAudit.Run(out report)`.
4. Вызвать `ArtBake.BakeActiveScene()` кодом. Для этой сцены ответ: Synty-зависимостей нет.
5. Собрать development macOS с Boot, Hub, CarryItem. Запустить
   `tools/carry-skyscraper-check.sh 4 4` или `tools/carry-skyscraper-check.sh 8 2`.
   Второй аргумент — сколько процессов рендерить, третий — ширина окна.

## Проверки

- [Аудит арта и коллизий](art-audit.txt): 372 опорных луча, 480 капсул маршрута,
  64 позиции вокруг выдачи, свободные 4.5 м позади выдачи, зависимости и компоненты.
- [Сетевой аудит](traffic-audit.txt): 31 прежний реплицируемый член, без новых каналов.
- [Повторная пересборка](rebuild-verification.txt).
- Подробные журналы и автоматические скриншоты находятся локально в `network-*`;
  эти большие диагностические каталоги исключены из Git.

## Найдено и исправлено по прогонам

- Сохранён импортный поворот FBX внутри обёртки: прежнее присваивание yaw
  укладывало стояк на бок и увеличивало коллизию до 4.39 м.
- Тачки вынесены из подходов, стояк перемещён к выходу из горлышка.
- Настил расширен с 1.44 до 3.24 м: четыре несущих помещаются по ширине.
- Штабели вынесены в боковые рабочие зоны; заднее место несущего больше
  не пересекается со штабелем.

Первый смешанный сетевой прогон также выявил клиентское падение без возврата.
Это наблюдение сохранено: `network-4-20260917-172928/client-3-probe.txt`.
После расширения настила полный прогон четырёх отображаемых клиентов
`network-4-20260917-173447` завершился 100:59, все четыре вернулись в хаб,
ошибок и проваливаний не было. Это промежуточный прогон до переноса штабелей.
Итоговый прогон `network-4-20260917-175854`: 198:100, доставки 2/1,
200 с, четыре участника; все получили одинаковый результат и вернулись в Hub.
Минимальная Y: −0.03 м, runtime-исключений нет.
[Сводка четырёх игроков](network-4-summary.json).
После этого прогона уточнены только небесное заполнение/skybox и стойки
флажков за штабелями; финальные ArtAudit и повторная пересборка пройдены.
Console при съёмке: 0 ошибок, два предупреждения Metal/ScreenCapture
о memoryless depth; при сборке — предупреждение Addressables
`ProfileValueReference: GetValue called with empty id`. Финальный Build
`build-72680e1e83`: Succeeded, 953.4 MB, 0 errors / 0 warnings.
Восемь одновременно отображаемых клиентов дали около 8 FPS на этой машине:
такой режим перегружает стенд и не годится для оценки обычной производительности.

## Блокер полной приёмки

[IGR-536](https://linear.app/igruha228/issue/IGR-536/1735-carryitem-klient-posle-padeniya-prodolzhaet-padat-vmesto-vozvrata):
падение клиента без возврата повторилось на итоговой геометрии, с двумя
отображаемыми окнами и шестью headless-клиентами, Unity Editor был закрыт.
[Диагностический журнал клиента](IGR-536-client-5-probe.txt). Причина и наличие
сбоя в main не установлены. Полная приёмка 4–8 остаётся открытой;
Core/Networking не изменялись по ограничению задачи.

## Кадры из Main Camera в Play Mode

Камера игровая, без замены рига. Для осмотра персонаж перемещён к контрольным
местам, поворот обзора менялся временно; эти положения в сцену не сохранялись.

![Старт](Final_01_Start.png)

![Мостки и нижние этажи](Final_02_Bridge.png)

![Горлышко](Final_03_Neck.png)

![Бак](Final_04_Tank.png)

![Краны и город](Final_05_Skyline.png)

## Второй проход — золотой час, город, живой мир (IGR-537, 18.09.2026)

Генератор `tools/blender/carry_skyscraper_city.py` добавляет 62 FBX и текстуры к тому же
набору `Art/CarryItem/Original` (исходник `Art/Source/CarrySkyscraper/CarrySkyscraperCity.blend`),
сливая `palette.json`. Пересборка — той же `CarryItemArenaBuilder.Rebuild()`.
Что сделано и почему — раздел 17 спеки `docs/minigames/carry-item.md`.

Кадры из Play Mode (настоящая Main Camera с постобработкой):

- `IGR537_01_Wide.png` — здание в городе, краны, облака, дымка по высоте.
- `IGR537_02_Route.png` — маршрут к горлышку: тени, стрелки, ядра, бак с флагом вдали.
- `IGR537_03_Tank.png` — зона бака команды B: бак-герой, флаг, гирлянда, реквизит.
- `IGR537_04_GameCamera.png` — игровая камера на старте.
- `IGR537_05_Edge.png` — взгляд с края вниз: кирпичные корпуса и улица в дымке.
- `IGR537_06_City.png` — общий вид снаружи (Scene-камера, без постобработки).

Аудит после пересборки — `art-audit-igr537.txt`: 496 капсул маршрутов крест-накрест,
64 станции хватания, 4.5 м камеры, 0 коллайдеров фона, 0 лишних автостартов.
