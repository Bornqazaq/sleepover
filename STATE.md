# STATE.md — где мы сейчас

> **Срез состояния проекта:** что работает, что в работе, что дальше. Читается в начале сессии целиком — он для этого и держится коротким.
>
> **История проходов — в `docs/history/`.** Целиком её читать не надо и не нужно: указатель `docs/history/README.md` даёт номер строки на каждый раздел, дальше читается ровно нужный диапазон (`sed -n 'A,Bp' docs/history/STATE-archive.md`).
>
> **Задачи и их статусы — в Linear** (команда `IGRUHA`, проект «Комната (Party-game)»), не здесь. Дублировать их сюда не надо: копия разъезжается с оригиналом.
>
> **Правила кода и замороженное** — `igruha/CLAUDE.md`. **Процесс, ветки, конвейер** — корневой `CLAUDE.md`.

---

## 1. Последний проход

**2026-09-18, IGR-566 — общий счёт катки.** Очки за мини-игры сведены в один контур. Формула одна на сетевое и локальное табло (`SessionScoring`): `очки = игроков на старте раунда − место` — состав берётся на старте, поэтому вышедшие посреди матча больше не обесценивают победу (закрыт давний IGR-372). Итоги раунда несут очки и сумму катки и едут клиентам одним RPC вместе с местами. В табло появился журнал раундов (`History`, `RoundsPlayed`), каждая засчитанная игра в нём записана.

Два режима считаются по-разному, по решению геймдизайнера:

- **«Полная игра»** (серия с телевизора) — очки копятся игра за игрой, на экране итогов `+N` и «всего»; после последней игры все машины показывают таблицу катки, затем хаб, где чемпион ходит с короной (`ChampionCrown`) и отмечен на карточке ТВ.
- **Одиночная игра** с телевизора — очки считаются только внутри неё и показывают победителя; сумма катки, журнал и корона не трогаются.

Равные суммы делят первое место и корону: тай-брейк боем подушками не собран (IGR-81).

**Проверено:** EditMode 24/24 своих тестов, компиляция чистая; стенд host + 2 client (ничья за корону) и host + 3 client на трёх играх подряд — итоги раундов, таблица катки и корона совпали на всех машинах, исключений нет. Влито в `main`, опубликовано.

**Не проверено:** живой плейтест (вид короны на персонажах, читаемость колонок очков), Windows, восемь живых игроков, дисконнект посреди серии.

---

## 2. Мини-игры

Десять игр существуют сценами и играются по сети под авторитетом сервера. «Каркас» — фаза 2 конвейера, «сеть» — фаза 3, «арт» — фаза 4 (первый проход, в основном паками Synty), «шлифовка» — повторный проход собственным артом через `/mg-polish`.

| Игра | Эпик | Каркас | Сеть | Арт | Шлифовка своим артом | Что осталось |
|---|---|---|---|---|---|---|
| **Дырка в стене** | IGR-190 | ✅ | ✅ | ✅ | ✅ солнечный аквапавильон (16–18.09) | живой плейтест |
| **Переноска предмета** | IGR-177 | ✅ | ✅ | ✅ | ✅ стройка небоскрёба (17–18.09) | **IGR-536** — падение клиента без возврата, держит приёмку 4–8 |
| **Рейс на память** | IGR-126 | ✅ | ✅ | ✅ | ✅ заводской цех (17.09), принят пользователем | живой плейтест |
| **Заражение** | IGR-538 | ✅ | ✅ | ✅ | ✅ сразу своим артом: апокалиптический двор (18.09) | живой плейтест, оценка арта пользователем |
| **Экзамен** | IGR-127 | ✅ | ✅ | ✅ | ✅ школьный зал (15.09), пользователь не оценивал | живой плейтест |
| **Секундомер** | IGR-109 | ✅ | ✅ | ✅ | ✅ ночной цирк и зверь Бруно (11–15.09) | живой прогон на 3–4 машинах |
| **Порядок банок** | IGR-110 | ✅ | ✅ | ✅ | ✅ тот же цирк, общая сцена ярмарки | живой прогон на свежих билдах |
| **Плачущие ангелы** | IGR-159 | ✅ | ✅ | ✅ | ✅ ночная галерея (09–11.09) | живой плейтест |
| **Верю / не верю** | IGR-191 | ✅ | ✅ | ✅ | ❌ не шлифовалась | живой прогон на людях |
| **Duck Hunt** | IGR-143 | ✅ | ✅ | ✅ Synty (27.08) | ❌ не шлифовалась | **ведёт напарник** — сцену и арену не трогать без просьбы |

Спеки всех десяти — в `docs/minigames/`, открытых вопросов в них нет.

**Не начаты вовсе:** EPIC 13 (прятки), 15 (камеры-ловушки), 16 (крокодил), 20 (бой подушками, он же тай-брейк).

---

## 3. Core — что уже готово и переиспользуется

Шаблон мини-игры работает, инструкция — `igruha/Assets/_Project/Scenes/MinigameTemplate_HOWTO.md`. Перед фазой 2 её надо перечитывать: там актуальный список того, что даётся бесплатно.

- **Персонаж:** `PlayerController` + `CharacterConfig`, толчки, нокдаун и вставание, респавн, `KillZone`, приседание, `MovementLocked`, `StuckDetector`.
- **Раунд:** `MinigameControllerBase` (фазы, таймер, сбор результатов, возврат в хаб), `MinigameDefinition`, `MinigameCatalog`, `MinigameLoader`, `RoundTimer`, `TutorialScreen`, `RoundHud`.
- **Места и очки:** `ScoreRanking` (по счёту), `EliminationRanking` (по порядку вылета), `TeamRanking` (командные половины), `SessionScoring` (формула очков), `SessionStandings` (таблица катки), `PartySeries` (серия игр), `ChampionCrown` (корона в хабе).
- **Спавн и роли:** `SpawnPoint`/`SpawnPointSet`/`PlayerSpawner` на 2–8, разнос по кольцу, `SpecialRoleHistory` (не давать одну особую роль дважды подряд).
- **Мир:** предметы (`PickupItem`, `PlayerCarryAbility`), снаряды, ловушки, взаимодействие по E, `VisionCone` + слой `Cover`, декоративные `Ambient`.
- **Камера и UI:** `MinigameCameraController`, `ThirdPersonCameraRig` (формат проекта), `FirstPersonCameraRig` (только Охотник), `SpectatorCamera`, колесо эмоций по Tab, пауза.

⚠️ Всё из раздела «🔒 ЗАМОРОЖЕНО» в `igruha/CLAUDE.md` (персонажи, камера, анимации, удар, колесо, клавиши) менять нельзя. Каждый пункт того списка уже ломался молча — разборы лежат в архиве.

---

## 4. Сеть

Авторитет сервера закрыт по всем основным механикам: движение и анимации, импульсы, approval и спавн, дисконнекты и выход хоста, фазы/таймер/результаты мини-игры, ловушки, `KillZone` и респавн, перенос предметов, кнопки и двери, снаряды, роли и очки катки.

- **Relay и Lobby отключены** (IGR-47, 55, 56) — прогоны идут на localhost и по Tailscale.
- Стенд Multiplayer Play Mode настроен, инструкция — `Scripts/Networking/NetworkTestGuide.md`.
- Матчи проверялись на 2–4 машинах и на стенде из 8 процессов. **Восьмерых живых людей не собирали ни разу.**

---

## 5. Хаб и вход в игры

Хаб — подвальная game room, собранная своими моделями (компактный вариант B). Вход в мини-игры один: телевизор с игровой приставкой. Предметы-якори отменены и удалены.

Экран ТВ: главное меню → «Полная игра» (комната участников, серия всех подходящих по составу игр в случайном порядке без повторов) либо «Выбрать мини-игру» (библиотека с обложками). Состояние экрана общее и живёт на сервере, водит по нему хост. Там же выбор персонажа и смена имени/облика.

---

## 6. Открытые долги

| Что | Где | Состояние |
|---|---|---|
| Падение клиента без возврата в «Переноске» | IGR-536 | открыт, держит приёмку 4–8 двух арт-проходов |
| Тай-брейк при равенстве очков | IGR-81 / EPIC 20 | не собран; пока равные суммы делят корону |
| Освистывание проигравшего в финале | IGR-80 | не делалось |
| Лобби и матч-флоу | EPIC 3 | не начато |
| Кастомизация персонажа | EPIC 7 | не начато |
| Relay + Lobby | IGR-47, 55, 56 | выключены, играем по localhost/Tailscale |
| Обложка «Заражения» в библиотеке ТВ | — | отсутствует, из-за неё падает `ConsolePresentationTests` |
| `HoleInWallScheduleTests` ждёт 75 с, расписание даёт 73.5 | — | тест не обновлён после IGR-533, падает 5 проверок |
| Вес репозитория в LFS | — | больше 5 ГБ; каждый клип анимации несёт внутри всю модель. Новые анимации класть без модели |
| Windows | — | в последних проходах не проверялся вовсе |

---

## 7. Стенды, билды, инструменты

**Unity без MCP** (редактор закрыт, `unity`/`UnityMCP` не подключились) — всё делается из терминала, редактор в `~/Unity/Hub/Editor/6000.3.11f1/Unity.app/Contents/MacOS/Unity`:

```bash
# компиляция + EditMode-тесты (~2 мин)
Unity -batchmode -nographics -projectPath igruha -runTests -testPlatform EditMode \
      -testResults out.xml -logFile out.log
# билд стенда в Builds/Autotest (~10 мин первый раз, ~30 с инкрементально)
Unity -batchmode -nographics -quit -projectPath igruha -buildTarget OSXUniversal \
      -executeMethod Igruha.EditorTools.AutotestBuild.BuildMac -logFile build.log
```

Два инстанса Unity на один проект одновременно не запускать. Batch-запуск переписывает `ProjectSettings/TimeManager.asset` — откатывать перед коммитом.

**Сетевые стенды** (нужен билд `Builds/Autotest`):

```bash
tools/autorun-series.sh 4 HoleInWall,Stopwatch,Exam   # серия катки: очки, таблица, корона
tools/autorun-exam.sh 8 Exam                          # одна игра на восьми процессах
tools/autorun-report.sh                               # сличить логи машин между собой
```

Стенд сам сравнивает строки `📊 итоги раунда` и `🏁 таблица катки` у хоста и клиентов: расхождение счёта иначе не поймать, у каждой машины своя правдоподобная картинка.

**Прочее:** звук генерируется через fal.ai прямо в проект (нормализация обязательна), модели и наборы — через Blender MCP, промо — Higgsfield. Mixamo с этой машины недоступен.

---

## 8. Грабли, которые уже стоили времени

Короткий список того, на что уходили часы. Разборы с числами — в архиве, ищутся по указателю.

- **Закрывать Unity перед `git pull` и `merge`.** Иначе редактор и git пишут в одни файлы, `.meta` пересоздаются, ссылки в префабах рвутся.
- **`NetworkConfig mismatch` — это расхождение списка сетевых префабов, а не сломанный хеш.** NGO дописывает найденные префабы в память редактора, не помечая ассет грязным; на диск это не уходит, клиент читает файл. После правки сетевых префабов — `SetDirty` + `SaveAssetIfDirty` и сверка числа позиций в файле с рантаймом.
- **Порт 7777 остаётся занятым** после сноса NGO перекомпиляцией посреди play. Лечится только полным закрытием Unity. Проверять `lsof -nP -iUDP:7777`, если хост «не поднимается».
- **Перекомпиляция посреди play молча сносит NGO:** оба редактора продолжают играть, но `IsServer = false` и состав пустой. Если замеры показывают пустой ростер — проверять это первым.
- **Unity не поднимается запуском бинарника из скрипта** (залипает на лицензии). Редактор запускает человек; скриптом можно только закрыть.
- **Через MCP нельзя возвращать большие строки и бросать исключения** — мост виртуального игрока умирает. Сворачивать замеры в сводку на стороне редактора.
- **Маска препятствий камеры — только сплошная геометрия** (`Ground`, `Cover`, `PlayerBarrier`). Стена на `Default` для камеры прозрачна, а триггеры на `Default` камера начнёт ловить.
- **Арену нельзя строить вплотную к стене:** персонажу нужно около 4.5 м позади, иначе камера прижимается к телу.
- **Перед сборкой арены сверять активную сцену.** После `refresh_unity` Unity может открыть чужую, а билдеры сносят `_Arena` по общему имени — так была испорчена чужая сцена.
- **Долгий вызов Unity MCP, вернувший `success:false, message:null`, — это обрыв websocket, а не ошибка.** Работа доходит до конца, проверять повторным вызовом, а не перезапускать вслепую.

---

## 9. Архив истории

Всё, что было до этого среза, лежит в `docs/history/STATE-archive.md` (680 разделов, вся хронология с августа). Указатель с номерами строк — `docs/history/README.md`.

**Как пополнять.** Свежий проход описывается в разделе 1 этого файла. Когда он перестаёт быть последним, содержательная часть переносится в конец архива, а здесь остаётся только то, что меняет текущее состояние: статус игры в таблице, новый долг, новые грабли. После переноса — `python3 tools/state_index.py`, он пересобирает указатель.

Так этот файл остаётся коротким, а история не теряется.
